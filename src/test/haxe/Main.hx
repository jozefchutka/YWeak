class Main
{
	static function main()
	{
		yweak.IterableWeakMapTest.all();
		/*
		final map = new yweak.IterableWeakMap<{}, String>();
		map.set({}, "1");
		map.set({}, "2");
		for (key => value in map)
			trace(key, value);
			*/
	}
}
